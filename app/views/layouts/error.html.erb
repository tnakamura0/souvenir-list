<!DOCTYPE html>
<html>
  <head>
    <title><%= t("errors.not_found.meta_title") %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <%= csp_meta_tag %>

    <%= favicon_link_tag "favicon.svg", type: "image/svg+xml" %>
    <%= favicon_link_tag "favicon.ico" %>

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
  </head>

  <body class="font-sans min-h-screen flex flex-col bg-background text-text-primary">
    <main class="container mx-auto px-5 flex-1 flex items-center justify-center max-w-2xl">
      <%= yield %>
    </main>
  </body>
</html>
