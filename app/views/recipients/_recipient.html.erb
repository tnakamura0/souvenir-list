<%= link_to recipient_path(recipient), class: "card card-hover block mb-3 p-5" do %>
  <%= render(
      "shared/recipient_summary",
      recipient: recipient,
      show_link: false
    ) %>
<% end %>
