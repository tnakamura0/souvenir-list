<% if object.errors.any? %>
  <ul class="p-3 text-sm text-danger bg-danger-soft border border-danger/70 rounded-xl">
    <% object.errors.each do |error| %>
      <li class="ml-3 list-disc"><%= error.full_message %></li>
    <% end %>
  </ul>
<% end %>
